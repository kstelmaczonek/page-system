# Page System

Requirements: Composer and Docker (with docker-compose) locally installed.

In app directory run:

* composer install
* docker-compose up -d
* symfony serve -d

Open http://127.0.0.1:8000/ in your browser.
